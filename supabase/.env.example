# Copy to supabase/.env (git-ignored) for local Edge Function development.
# NEVER commit real values.

# Server-side only — the image-generation provider key (ADR 0001).
# The client never sees this.
GEMINI_API_KEY=

# Fallback provider (optional).
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
